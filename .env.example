# =============================================================================
# PDF FIXER FRONTEND CONFIGURATION
# =============================================================================

# Backend API URL (leave empty for same host / reverse proxy, or point to FastAPI)
VITE_API_BASE_URL=http://localhost:8000
